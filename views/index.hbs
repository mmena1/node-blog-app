<!DOCTYPE html>
<html>

<head>
    <meta charset="utf-8">
    <title>{{pageTitle}}</title>
</head>

<body>
    {{> header}}
    <p>All posts go here...</p>
    <div>
        {{#if message}} {{message}} {{/if}} {{#each postsWithUsers}}
        <article>
            <h1>{{post.title}}</h1>
            <p>{{post.content}}</p>
            <p>
                <small>Author: {{user.username}}</small>
            </p>
        </article>
        <hr> {{/each}}
    </div>
</body>
{{> footer}}

</html>