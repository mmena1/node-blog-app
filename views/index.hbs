<!DOCTYPE html>
<html>

<head>
    <meta charset="utf-8">
    <title>{{pageTitle}}</title>
</head>

<body>
    {{> header}}
    <p>All posts go here...</p>
    <div>
        {{#if message}} {{message}} {{/if}} {{#each posts}}
        <article>
            <h1>{{title}}</h1>
            <p>{{content}}</p>
            <p>
                <small>Author: {{userId}}</small>
            </p>
        </article>
        <hr> {{/each}}
    </div>
</body>
{{> footer}}

</html>